'use strict';Object.defineProperty(exports,'__esModule',{value:true});class MathUtilities{static sumNumArray(numArray){let result=0;for(let i=0;i<numArray.length;i++){result=result+numArray[i];}return result;}static weightedRandom(values,weights){let sum=MathUtilities.sumNumArray(weights);let rand=Math.floor(Math.random()*sum)+1;let i=0;let n=0;while(i<values.length){n=n+weights[i];if(n>=rand){break;}i++;}return values[i];}}exports.MathUtilities=MathUtilities;