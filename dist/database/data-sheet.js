'use strict';Object.defineProperty(exports,'__esModule',{value:true});class DataSheet{constructor(){this.fieldTypes={};this.fieldCount=0;}parseFields(fields){this.fieldCount=0;for(let index in fields){let obj=fields[index];let name='';let type='';for(let prop in obj){if(obj.hasOwnProperty(prop)){if(prop==='name')name=obj[prop];if(prop==='type')type=obj[prop];}}this.fieldTypes[name]=Number(type);this.fieldCount++;}console.log('FIELD COUNT:',fields.length);}getFieldType(fieldName){let result=-1;for(let name in this.fieldTypes){if(name==fieldName){result=this.fieldTypes[name];break;}}return result;}}exports.DataSheet=DataSheet;